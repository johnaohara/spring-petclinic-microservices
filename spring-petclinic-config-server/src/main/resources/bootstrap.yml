server.port: 8888
quarkus:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/quarkus-petclinic/quarkus-petclinic-microservices-config
---
quarkus:
  profiles: local
  cloud:
    config:
      server:
        git:
          uri: file:///${GIT_REPO}
